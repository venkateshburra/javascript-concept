// const para = document.querySelector('.abc');

// // para.innerHTML = ' <h1>Mahesh Burra</h1>';

// para.innerText = 'Navya Burra';

// // para.innerHTML = '<h1>Navya Burra</h1>'

// console.log(para);   

// let str = 'venky';

// function myfun(str) {
//   return str.split('').sort((a, b) => {
//     return (a > b) ? 1 : -1;
//   }).join('');
// }

// console.log(myfun(str));


// let str = 'venky';

// function myfun(str) {
//   let v = str.split('')
//   let b = v.sort(function(a, b) {
//     return (a > b) ? 1 : -1;
//   }).join('');
//   console.log(b);
// }

// // console.log(myfun(str));
// myfun(str);

